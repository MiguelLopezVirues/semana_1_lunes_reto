def mayor_suma_lista(lista):
    sublista = []
    maxi = max(lista)
    izq = None
    dcha = None
    # bucle decha
    for n in range(len(lista.index(maxi)),len(lista)):
        sublista = lista[lista.index(maxi):n+1]

    # bucle izq


    return sublista 